{% extends '::index.html.twig' %}

{% block body -%}
    <h1>CrimeQuestions list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Question</th>
                <th>Questionnumber</th>
                <th>Type</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('crimequestions_show', { 'id': entity.id }) }}">{{ entity.question }}</a></td>
                <td>{{ entity.questionNumber }}</td>
                <td>{{ entity.type }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('crimequestions_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('crimequestions_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('crimequestions_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
