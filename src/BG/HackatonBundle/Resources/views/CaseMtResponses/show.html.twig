{% extends '::index.html.twig' %}

{% block body -%}
    <h1>CaseMtResponses</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Message</th>
                <td>{{ entity.message }}</td>
            </tr>
            <tr>
                <th>Destaddress</th>
                <td>{{ entity.destAddress }}</td>
            </tr>
            <tr>
                <th>Senttime</th>
                <td>{{ entity.sentTime|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('casemtresponses') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('casemtresponses_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
